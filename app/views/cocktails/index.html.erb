
<div class="cocktail_header">
  <h1>Cocktail List</h1>  
  <%= link_to "Create New Cocktail", new_cocktail_path, class: "btn btn-primary btn-sm" %>  
</div>

<ul>
  <% @cocktails.each do |cocktail| %>
  <li><%= link_to cocktail.name, cocktail_path(cocktail) %> - <%= link_to "🗑", cocktail_path(cocktail), method: :delete, data: { confirm: "Are you sure?" } %></li>
  <% end %>
</ul>

